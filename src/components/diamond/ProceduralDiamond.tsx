'use client';
import { useState } from 'react'; import { DiamondCanvas } from './DiamondCanvas'; import { Controls } from './Controls'; import type { ProceduralSettings } from './types';
const defaults:ProceduralSettings={ior:2.42,brightness:1,contrast:1.15,rainbow:.65,speed:.18};
export function ProceduralDiamond(){const [settings,setSettings]=useState(defaults);return <div className="demo-grid"><div className="viewport"><DiamondCanvas mode="procedural" settings={settings}/><div className="viewport-label"><span>SDF · RAY MARCHED</span><span>DRAG TO ROTATE</span></div></div><Controls value={settings} onChange={setSettings} onReset={()=>setSettings(defaults)} fields={[{key:'ior',label:'Index of refraction',min:1.1,max:2.8,step:.01},{key:'brightness',label:'Brightness',min:.4,max:1.8,step:.01},{key:'contrast',label:'Contrast',min:.6,max:2,step:.01},{key:'rainbow',label:'Dispersion',min:0,max:1,step:.01},{key:'speed',label:'Animation speed',min:0,max:.6,step:.01}]}/></div>}
