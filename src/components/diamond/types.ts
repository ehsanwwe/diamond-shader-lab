export type MeshSettings = { ior:number; dispersion:number; tint:number; brightness:number; rotation:number };
export type ProceduralSettings = { ior:number; brightness:number; contrast:number; rainbow:number; speed:number };
