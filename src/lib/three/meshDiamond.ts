import * as THREE from 'three'; import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js'; import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js'; import { assetPath } from '@/lib/assets/path'; import { backfaceFragment,backfaceVertex,diamondFragment,diamondVertex } from '@/shaders/mesh'; import { fitRenderer,lifecycle,rendererFor } from './common'; import type { MeshSettings } from '@/components/diamond/types';
export function createMeshDiamond(canvas:HTMLCanvasElement,get:()=>MeshSettings|object,onReady:()=>void,onError:()=>void){const renderer=rendererFor(canvas),scene=new THREE.Scene(),backScene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(38,1,.05,30);camera.position.set(0,.35,3.4);const controls=new OrbitControls(camera,canvas);controls.enableDamping=true;controls.minDistance=2;controls.maxDistance=6;controls.maxPolarAngle=Math.PI*.82;let target=new THREE.WebGLRenderTarget(1,1,{type:THREE.HalfFloatType});let front:THREE.Mesh|undefined,back:THREE.Mesh|undefined;const backMat=new THREE.ShaderMaterial({vertexShader:backfaceVertex,fragmentShader:backfaceFragment,side:THREE.BackSide});const uniforms={uBackface:{value:target.texture},uResolution:{value:new THREE.Vector2(1,1)},uIor:{value:2.42},uDispersion:{value:.04},uTint:{value:0},uExposure:{value:1}};const frontMat=new THREE.ShaderMaterial({vertexShader:diamondVertex,fragmentShader:diamondFragment,uniforms,transparent:true,side:THREE.FrontSide});
 new GLTFLoader().load(assetPath('models/ehsan.gltf'),g=>{let found:THREE.Mesh|undefined;g.scene.traverse(o=>{if(o instanceof THREE.Mesh&&((o.material instanceof THREE.Material&&o.material.name==='Diamond_main')||o.name==='Diamond'))found=o});if(!found){onError();return;}const geometry=found.geometry.clone();geometry.computeBoundingBox();const box=geometry.boundingBox!;const center=box.getCenter(new THREE.Vector3());geometry.translate(-center.x,-center.y,-center.z);const scale=1.4/box.getSize(new THREE.Vector3()).length();front=new THREE.Mesh(geometry,frontMat);back=new THREE.Mesh(geometry.clone(),backMat);front.scale.setScalar(scale);back.scale.copy(front.scale);scene.add(front);backScene.add(back);onReady();},undefined,onError);
 const resize=()=>{const {w,h,dpr}=fitRenderer(renderer,canvas);camera.aspect=w/h;camera.updateProjectionMatrix();target.dispose();target=new THREE.WebGLRenderTarget(Math.floor(w*dpr),Math.floor(h*dpr),{type:THREE.HalfFloatType});uniforms.uBackface.value=target.texture;uniforms.uResolution.value.set(w*dpr,h*dpr)};resize();const stop=lifecycle(renderer,canvas,t=>{const s=get() as MeshSettings;uniforms.uIor.value=s.ior;uniforms.uDispersion.value=s.dispersion;uniforms.uTint.value=s.tint;uniforms.uExposure.value=s.brightness;if(front&&back){front.rotation.y+=s.rotation*.003;back.rotation.copy(front.rotation)}controls.update();renderer.setRenderTarget(target);renderer.clear();renderer.render(backScene,camera);renderer.setRenderTarget(null);renderer.render(scene,camera)},resize,onError);return{dispose(){stop();controls.dispose();target.dispose();front?.geometry.dispose();back?.geometry.dispose();frontMat.dispose();backMat.dispose();}}}
