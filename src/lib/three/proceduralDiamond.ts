import * as THREE from 'three'; import { fitRenderer,lifecycle,rendererFor } from './common'; import { fullscreenVertex,proceduralFragment } from '@/shaders/procedural'; import type { ProceduralSettings } from '@/components/diamond/types';
export function createProceduralDiamond(canvas:HTMLCanvasElement,get:()=>ProceduralSettings|object,onReady:()=>void,onError:()=>void){const renderer=rendererFor(canvas),scene=new THREE.Scene(),camera=new THREE.Camera(),pointer=new THREE.Vector2(.35,-.15),target=pointer.clone();const uniforms={uResolution:{value:new THREE.Vector2()},uPointer:{value:pointer},uTime:{value:0},uIor:{value:2.42},uBrightness:{value:1},uContrast:{value:1.15},uRainbow:{value:.65}};const material=new THREE.ShaderMaterial({vertexShader:fullscreenVertex,fragmentShader:proceduralFragment,uniforms});const geometry=new THREE.PlaneGeometry(2,2);scene.add(new THREE.Mesh(geometry,material));let dragging=false,last=new THREE.Vector2();const down=(e:PointerEvent)=>{dragging=true;last.set(e.clientX,e.clientY);canvas.setPointerCapture(e.pointerId)};const move=(e:PointerEvent)=>{if(!dragging)return;target.x+=(e.clientX-last.x)*.008;target.y=Math.max(-1.2,Math.min(1.2,target.y+(e.clientY-last.y)*.008));last.set(e.clientX,e.clientY)};const up=()=>dragging=false;canvas.addEventListener('pointerdown',down);canvas.addEventListener('pointermove',move);canvas.addEventListener('pointerup',up);const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const resize=()=>{const {w,h,dpr}=fitRenderer(renderer,canvas);uniforms.uResolution.value.set(w*dpr,h*dpr)};resize();onReady();const stop=lifecycle(renderer,canvas,t=>{const s=get() as ProceduralSettings;if(!dragging&&!reduced)target.x+=s.speed*.00025;pointer.lerp(target,.09);uniforms.uTime.value=t*.001;uniforms.uIor.value=s.ior;uniforms.uBrightness.value=s.brightness;uniforms.uContrast.value=s.contrast;uniforms.uRainbow.value=s.rainbow;renderer.render(scene,camera)},resize,onError);return{dispose(){stop();canvas.removeEventListener('pointerdown',down);canvas.removeEventListener('pointermove',move);canvas.removeEventListener('pointerup',up);geometry.dispose();material.dispose();}}}
 
